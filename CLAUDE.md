# AGENTS.md

## 프로젝트

타임라인 댓글 기반 구간 스킵 크롬 확장.

유튜브 영상의 타임라인 댓글(`00:01 곡명` 형식)을 파싱해서 플레이어 옆에 트랙 목록으로 띄운다.
사용자가 체크를 해제한 트랙은 재생 중 자동으로 건너뛴다. 사용자가 직접 타임라인을 추가하거나
표시 텍스트를 수정할 수도 있다.

1차 타겟은 유튜브. 치지직·숲은 이후에 추가한다. 따라서 **플랫폼에 의존하는 코드는 반드시
`src/adapters/` 안에 가둔다.** `core/`에 유튜브라는 단어가 등장하면 설계가 잘못된 것이다.

## 스택

- 순수 JavaScript (ESM), Manifest V3
- 빌드 단계 없음. 소스가 곧 실행되는 코드다.
- content script는 정적 `import`를 못 쓴다. `src/content.js`는 동적 import로 모듈을 불러오는
  **로더 역할만** 한다. 불러온 모듈은 `src/wiring.js`의 `start()`에 넘기고, 조립은 거기서 한다.
  불러오는 파일은 `manifest.json`의 `web_accessible_resources`에 등록한다.
- 테스트: Vitest. npm은 개발 도구용일 뿐, 확장 런타임에는 어떤 npm 패키지도 들어가지 않는다.

## 구조

```
manifest.json
src/
  content.js          # 로더. 모듈을 불러와 wiring.start()에 넘기는 일만 한다
  wiring.js           # 조립. state를 만들고 core / adapters / ui를 연결한다. 로직 금지
  trackActions.js     # 목록을 바꾸는 동작 모음. state를 고치고 commit으로 끝난다
  core/               # 순수 로직. document / window / chrome 참조 금지
  adapters/           # 플랫폼 경계 (DOM 조회, 플레이어 제어, storage)
    selectors.js      # 모든 DOM 셀렉터는 여기에만
  ui/                 # 패널 렌더링 및 이벤트 바인딩
tests/
  core/               # core 구조를 그대로 미러링
```

### 레이어 규칙 (이 문서에서 가장 중요)

- `core/`는 브라우저 API를 **절대** 참조하지 않는다. 입력은 문자열이나 값 객체, 출력도 값이다.
  이 규칙 덕분에 core는 전부 단위 테스트로 검증되고, 사람이 코드를 읽지 않아도 리뷰가 성립한다.
- DOM 셀렉터 문자열은 `adapters/selectors.js`에만 존재한다. 다른 파일에 하드코딩하지 않는다.
  유튜브 마크업이 바뀌면 이 파일 하나만 고치면 되어야 한다.
- `wiring.js`는 core / adapters / ui를 연결만 한다. 조건 분기와 계산은 여기 두지 않는다.
  `content.js`에는 모듈 경로 목록 말고 아무것도 두지 않는다.

## 명령어

- `npm install` — 최초 1회
- `npm test` — 전체 테스트
- `npx vitest run tests/core/parseTimelineComment.test.js` — 단일 파일
- 빌드 명령은 없다.
- 동작 확인: 사람이 `chrome://extensions` → 개발자 모드 → 압축해제된 확장 프로그램 로드 →
  코드 수정 후 새로고침.

**에이전트는 브라우저에서 동작을 확인할 수 없다.** 실제 동작 확인은 항상 사람이 한다.
따라서 "동작합니다"라고 단정하지 말고, 무엇이 테스트로 검증됐고 무엇이 미확인인지 구분해서 보고한다.

## 코드 규칙

- 파일 하나에 책임 하나. 파일당 export하는 주 함수는 1~2개.
- 함수 40줄, 파일 150줄을 넘기지 않는다. 넘겨야 한다면 먼저 이유를 말한다.
- 이름을 축약하지 않는다. `ts`가 아니라 `timestampSeconds`.
- 주석은 "무엇을 하는지"가 아니라 "왜 이렇게 했는지"만 쓴다.
- 시간은 내부적으로 항상 초 단위 숫자로 다룬다. `"04:29"` 같은 표시 형식과의 변환은 경계에서만 한다.
- 전역 변수를 만들지 않는다. 상태는 명시적으로 전달한다.

## 테스트 규칙

- `core/`의 모든 export 함수는 테스트가 있어야 한다. **테스트 없는 core 코드는 완료가 아니다.**
- 테스트 이름은 한글 서술문으로 쓴다. 테스트 목록만 읽어도 그 함수가 무엇을 보장하는지 알 수 있어야 한다.

```js
describe('parseTimelineComment', () => {
  it('"00:01 Ballerino" 형식의 줄을 시각과 제목으로 분리한다', ...)
  it('"1:02:33" 처럼 시간 단위가 있는 형식도 초로 변환한다', ...)
  it('타임라인 형식이 아닌 줄은 결과에서 제외한다', ...)
  it('타임라인이 하나도 없으면 빈 배열을 반환한다', ...)
})
```

- 준비 / 실행 / 검증 사이는 빈 줄로 구분한다. given-when-then 주석은 쓰지 않는다.
- 경계 케이스를 반드시 포함한다: 빈 입력, 시각이 역순인 경우, 중복 시각,
  마지막 트랙(끝 시각이 없음), 영상 길이를 넘는 시각.
- `adapters/`와 `ui/`는 자동 테스트를 요구하지 않는다. 대신 사람이 브라우저에서 확인할 수 있도록
  **확인 절차를 글로 남긴다** (어떤 영상에서, 무엇을 눌러서, 무엇이 보이면 성공인지).

## 작업 방식

- 한 번에 한 가지만 한다. 변경 범위는 파일 3개 이내를 기본으로 한다.
- 구현 전에 계획을 먼저 제시한다: 건드릴 파일, 추가할 함수, **작성할 테스트 이름 목록**.
  승인을 받은 뒤에 구현한다.
- `core/` 작업은 테스트를 먼저 쓰고, 실패하는 것을 확인시킨 다음 구현한다.
- 요청 범위 밖은 건드리지 않는다. 개선할 점이 보이면 코드를 고치지 말고 말로 제안한다.
- 작업을 마치면 세 가지를 보고한다: 무엇을 바꿨는지 / 무엇이 테스트로 검증됐는지 /
  사람이 브라우저에서 무엇을 확인해야 하는지.

## 승인 없이 하지 않는 것

- `manifest.json`의 `permissions`, `host_permissions`, `matches` 추가나 변경
- 런타임 의존성 추가, 번들러·빌드 도구 도입
- 파일 삭제, 디렉토리 구조 변경
- `git commit`, `git push`
- 기존 테스트의 기대값 수정 — 테스트가 틀렸다고 판단되면 고치기 전에 먼저 말한다

## 모를 때

유튜브 DOM 구조, 확장 API 동작, 플레이어 이벤트 이름을 추측해서 채워 넣지 않는다.
확실하지 않으면 멈추고, 무엇을 어디서 확인해야 하는지(어떤 셀렉터를 개발자 도구에서 확인할지 등)를
제시한다. 추측한 셀렉터로 만든 코드는 조용히 실패하기 때문에 가장 비싸다.