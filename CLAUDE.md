# CLAUDE.md

## 프로젝트

유튜브·치지직 영상의 타임라인 댓글(`00:01 곡명`)을 읽어 플레이어 옆에 트랙 목록으로 띄우는 크롬 확장.
체크를 해제한 트랙은 재생 중 건너뛴다. 트랙을 직접 추가하거나 제목을 고칠 수도 있다.
숲은 이후에 추가한다.

## 스택

- 순수 JavaScript (ESM), Manifest V3. 빌드 단계가 없어 소스가 곧 실행되는 코드다.
- 확장 런타임에는 npm 패키지가 하나도 들어가지 않는다. npm은 테스트 도구용이다.
- content script는 정적 `import`를 못 쓴다. 그래서 `content.js`가 모듈을 동적 import로 불러오고,
  불러오는 파일은 `manifest.json`의 `web_accessible_resources`에 등록한다.

## 구조

```
src/
  core/          # 계산만 하는 코드. 화면이나 브라우저를 모른다
  adapters/      # 유튜브·치지직 등 플랫폼 페이지와 브라우저 기능(영상, 저장소, 클립보드 등)을 직접 만지는 코드
  ui/            # 화면에 그리는 코드
  content.js     # 확장이 페이지에서 시작되는 파일
  wiring.js      # core / adapters / ui를 잇는 곳
  background.js  # 페이지 밖에서 도는 확장 코드
  popup/         # 확장 아이콘을 누르면 뜨는 창
tests/
  core/          # 단위 테스트
  adapters/      # adapters의 단위 테스트
  ui/            # jsdom 테스트
  e2e/           # 픽스처 페이지에 확장을 얹고 도는 테스트
  live/          # 진짜 유튜브·치지직 등 플랫폼 페이지에 붙는 테스트
  fixtures/      # 픽스처 페이지, 가짜 플레이어·저장소
```

### 레이어 규칙 (이 문서에서 가장 중요)

- `core/`는 브라우저를 모른다. `document`·`window`·`chrome`을 쓰지 않고, 값을 받아 값을 돌려준다.
  그래서 core는 전부 단위 테스트로 확인된다.
- 플랫폼마다 달라지는 코드는 `adapters/`에만 둔다. `core/`에 플랫폼 종속적인 단어가 나오면 설계가 잘못된 것이다.
- 유튜브·치지직 등 플랫폼 페이지의 셀렉터와 플랫폼별 차이는 `adapters/platformProfiles.js`에만 둔다.
  플랫폼이 화면 구조를 바꾸면 이 파일만 고치면 되어야 한다.
- `wiring.js`는 잇기만 한다. 조건 분기와 계산을 두지 않는다.
- `content.js`는 모듈을 불러오기만 한다. 불러올 목록은 `modulePaths.js` 한 곳에만 둔다.

## 명령어

- `npm test` — 단위·jsdom 테스트 (Vitest)
- `npm run test:e2e` — 픽스처 페이지에 확장을 얹은 크로미움에서 도는 테스트 (Playwright)
- `npm run test:live` — 실제 플랫폼 페이지 테스트. CI에서는 돌지 않는다
- 파일 하나만: `npx vitest run <파일>`, `npx playwright test <파일>`
- 사람이 확인할 때: `chrome://extensions` → 개발자 모드 → 압축해제된 확장 프로그램 로드 → 코드 수정 후 새로고침

**개발 중에는 `npm test`와 건드린 기능의 spec 파일 하나만 돌린다.** e2e 전체는
PR을 만들기 직전에 한 번 돌리고, 나머지는 CI에 맡긴다.

## 코드 규칙

- 파일 하나에 책임 하나. 파일당 export하는 주 함수는 1~2개.
- 함수 40줄, 파일 150줄을 넘기지 않는다. 넘겨야 한다면 먼저 이유를 말한다.
- 이름을 축약하지 않는다. `ts`가 아니라 `timestampSeconds`.
- 주석은 "무엇을 하는지"가 아니라 "왜 이렇게 했는지"만 쓴다.
- 시간은 안에서 늘 초 단위 숫자로 다룬다. `"04:29"` 같은 표시 형식으로 바꾸는 일은
  화면에 보여 주거나 입력을 받는 곳에서만 한다.
- 전역 변수를 만들지 않는다. 상태는 인자로 넘긴다.

## 테스트 규칙

### 공통

- 테스트 이름은 한글 서술문으로 쓴다. 테스트 목록만 읽어도 무엇을 보장하는지 알 수 있어야 한다.

```js
describe('parseTimelineComment', () => {
  it('"00:01 Ballerino" 형식의 줄을 시각과 제목으로 분리한다', ...)
  it('"1:02:33" 처럼 시간 단위가 있는 형식도 초로 변환한다', ...)
  it('타임라인 형식이 아닌 줄은 결과에서 제외한다', ...)
  it('타임라인이 하나도 없으면 빈 배열을 반환한다', ...)
})
```

- 준비 / 실행 / 검증 사이는 빈 줄로 구분한다. given-when-then 주석은 쓰지 않는다.
- 러너는 확장자로 갈린다 — Vitest는 `*.test.js`, Playwright는 `*.spec.js`.

### 단위 테스트 (`tests/core/`)

- `core/`의 모든 export 함수는 테스트가 있어야 한다. **테스트 없는 core 코드는 완료가 아니다.**
- 경계 케이스를 반드시 포함한다: 빈 입력, 시각이 역순인 경우, 중복 시각,
  마지막 트랙(끝 시각이 없음), 영상 길이를 넘는 시각.

### jsdom 테스트 (`tests/ui/`)

- 누르면 무엇이 바뀌나, 어떤 글자·속성이 들어가나. 한 기능의 모든 경우(경계값 포함)는 여기서 다룬다.
- 파일 맨 위에 `// @vitest-environment jsdom`을 적는다.
- 플레이어와 저장소는 `tests/fixtures/fakes/`의 가짜를 `wiring.start()`에 넘긴다.
  - 재생에 따른 동작(스킵, 반복)은 가짜 플레이어로 시각을 흘려 확인한다.
  - 새로고침 뒤 유지는 같은 가짜 저장소로 `start()`를 다시 불러 확인한다.
- 색 값은 CSS 파일을 글자로 읽어 확인한다 (`tests/ui/themeTokens.test.js` 방식).
  그 색이 화면에 실제로 입혀지는지는 e2e의 `[화면 보이나]`다.

### e2e (`tests/e2e/`)

- e2e는 느리다. 테스트 하나가 jsdom 테스트 수십 개만큼 걸린다.
  그래서 **jsdom으로는 확인할 수 없는 것만** e2e에 둔다. 아래 넷 중 하나여야 한다.
  - `[화면 보이나]` 위치, 크기, 겹침, 잘림, 마우스를 올렸을 때의 모양, 실제로 입혀진 색
  - `[브라우저 기능 되나]` 영상 재생·이동, 저장소, PiP, 클립보드, 전체화면.
    jsdom은 이것들을 가짜로 대신하므로, 가짜가 진짜와 같게 움직이는지는 여기서만 확인된다
  - `[조작 먹히나]` 마우스로 끌기, 메뉴 밖을 눌러 닫기, 초점 이동
  - `[확장 켜지나]` 확장이 페이지에 붙어 패널이 뜨고, 콘솔 오류가 없다
- "누르면 무엇이 바뀌나", "어떤 글자가 들어가나"는 위 넷이 아니다. jsdom에 쓴다.
- `[화면 보이나]`는 위치·크기·겹침·색을 재는 테스트다. 있는지·보이는지만 확인하면 jsdom이다.
- 기능 하나에 e2e는 하나다. 같은 기능의 여러 경우는 jsdom에서 다룬다.

### 실제 플랫폼 테스트 (`tests/live/`)

- 픽스처는 지난번에 본 화면 구조를 재현할 뿐이라, 플랫폼이 구조를 바꿔도 e2e는 전부 통과한다.
- 그래서 플랫폼마다 실제 페이지에 붙는 테스트를 하나씩 둔다.
  `platformProfiles.js`의 셀렉터가 오늘도 맞는지는 이것만이 답한다.

## 작업 방식

- 한 번에 한 가지만 한다.
- 구현 전에 계획을 먼저 제시한다: 건드릴 파일, 추가할 함수, **작성할 테스트 이름 목록**.
  승인을 받은 뒤에 구현한다.
- 계획의 e2e 테스트에는 이름 앞에 `[화면 보이나]`·`[브라우저 기능 되나]`·`[조작 먹히나]`·`[확장 켜지나]`
  중 하나를 붙인다. 붙일 수 없으면 jsdom 테스트다. 한 기능에 e2e를 둘 이상 두려면 이유를 먼저 말한다.
- 테스트를 먼저 쓰고, 실패하는 것을 확인한 다음 구현한다.
- 요청 범위 밖은 건드리지 않는다. 개선할 점이 보이면 코드를 고치지 말고 말로 제안한다.
- 작업을 마치면 세 가지를 보고한다: 무엇을 바꿨는지 / 무엇이 테스트로 검증됐는지 /
  사람이 브라우저에서 무엇을 확인해야 하는지.
  "동작합니다"라고 단정하지 않는다. 픽스처 위에서 통과한 것은 실제 플랫폼에서도 된다는 뜻이 아니다.

## GitHub 규약

작업의 종류(type)는 Angular 규약의 6가지다. 라벨 이름이자 커밋·브랜치의 앞부분이다.

`feat` 새 기능 · `fix` 버그 수정 · `refactor` 겉보기 동작이 그대로인 구조 개선 ·
`test` 테스트 추가·정비 · `docs` 문서만 변경 · `chore` 빌드·CI·설정

- **이슈·PR의 type은 라벨로만 표기한다.** 제목에 `feat:` 같은 prefix를 붙이지 않는다.
  목록에서 필터가 되는 것은 라벨뿐이다.
- 이슈와 PR에는 type 라벨을 **정확히 하나**, 건드린 파일이 아니라 목적을 기준으로 붙인다.
  기능에 딸린 테스트는 `feat`이고, `test`는 프로덕션 코드를 건드리지 않는 작업이다.
  목적이 정말 둘이면 라벨을 둘 붙이지 말고 작업을 쪼갠다.
- **커밋 메시지와 브랜치 이름에는 반대로 prefix를 쓴다.** `fix: 여백을 준다`,
  `<type>/#<이슈번호>`. `git log`에는 라벨이 없어서 prefix가 유일한 신호다.
- `manifest.json`의 `version`은 `/release`에서만 올린다.

### 이슈와 PR 제목·본문

- **제목은 사용자가 알아볼 결과를 명사형으로 짧게 쓴다.** 릴리스 노트에 그대로 들어간다.
  `치지직 다시보기 지원`, `빼기 버튼 hover 강조`
- **"무엇을 바꿨나"에는 무엇이 달라졌는지를 쉬운 말로 쓴다.** 파일 이름·셀렉터 같은 세부는
  diff에 있으니 쓰지 않는다.
- **결정을 쓰고 과정은 쓰지 않는다.** "왜 이렇게 했나"는 쓰고 "어떻게 그 결론에 이르렀나"는
  쓰지 않는다. "앞서", "예상보다", "처음엔" 같은 말이 나오면 과정을 쓰고 있는 것이다.
  읽는 사람은 그 대화에 없었다.
- **놀랄 만한 것만 설명한다.** 코드만 봐서는 이유를 알 수 없는 것만 쓴다.
  관례대로 한 선택에는 이유를 붙이지 않는다.
- **측정값은 숫자만 쓴다.** `24.6초 → 11.7초`. 그 숫자에 대한 소감은 쓰지 않는다.
- **테스트 이름을 본문에 나열하지 않는다.** 코드에 있다. 결과만 쓴다 (`npm test 133개 통과`).
- 산문은 **20줄을 넘기지 않는다.** 넘겨야 한다면 먼저 이유를 말한다.
  목록·표·코드블록은 세지 않는다.
- 이슈의 완료 조건에는 **실제로 검증할 것만** 적는다. 검증하지 않을 문장은 조건이 아니라 바람이다.

## 승인 없이 하지 않는 것

- `manifest.json`의 `permissions`, `host_permissions`, `matches` 추가나 변경
- 의존성 추가 (런타임은 물론 테스트용 개발 도구도), 번들러·빌드 도구 도입
- 파일 삭제, 디렉토리 구조 변경
- `main` 브랜치에 직접 push (작업 브랜치에서의 commit/push는 자유)
- 기존 테스트의 기대값 수정 — 테스트가 틀렸다고 판단되면 고치기 전에 먼저 말한다
- `gh issue create` — 이슈는 `/task` 로 시작할 때만 만든다.
  대화 중에 이슈로 만들 거리가 보이면 만들지 말고 말로 제안한다.

## 모를 때

플랫폼의 화면 구조, 확장 API 동작, 플레이어 이벤트 이름을 추측해서 채워 넣지 않는다.
확실하지 않으면 멈추고, 무엇을 어디서 확인하면 되는지 말한다 (예: 개발자 도구에서 볼 셀렉터).
추측한 셀렉터로 만든 코드는 조용히 실패해서 가장 비싸다.
